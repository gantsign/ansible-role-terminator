---
ansible:
  playbook: playbook.yml

docker:
  containers:
  - name: ansible-role-terminator-01
    ansible_groups:
      - group1
    image: ubuntu
    image_version: latest
  - name: ansible-role-terminator-02
    ansible_groups:
      - group2
    image: ubuntu
    image_version: latest
